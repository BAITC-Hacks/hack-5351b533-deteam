# Скопировать в .env.local и поправить
# true — фронт работает на src/api/mock.ts без бэкенда; false — ходит в настоящий API
VITE_USE_MOCK=true
# Куда dev-сервер проксирует /api
VITE_BACKEND_URL=http://localhost:8000
